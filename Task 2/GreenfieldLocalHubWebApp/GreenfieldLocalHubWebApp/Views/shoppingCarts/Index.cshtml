@model IEnumerable<GreenfieldLocalHubWebApp.Models.shoppingCart>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.shoppingCartCreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.shoppingCartStatus)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.shoppingCartCreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.shoppingCartStatus)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.shoppingCartId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.shoppingCartId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.shoppingCartId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
